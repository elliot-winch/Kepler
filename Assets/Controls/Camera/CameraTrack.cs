using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/*
[RequireComponent(typeof(TrackTarget))]
public class CameraTrack : CameraControlScheme
{
    //private MouseSelect<GravitationalBody> mouseSelect;
    private TrackTarget tt;

    protected override void Awake()
    {
        base.Awake();

        tt = GetComponent<TrackTarget>();

        /*
        mouseSelect = new MouseSelect<GravitationalBody>(camera)
        {
            OnLeftDoubleClick = gb => tt.target = gb.WorldTransform
        };
        
    }

    private void Update()
    {
        //mouseSelect.Update();
    }
}
*/